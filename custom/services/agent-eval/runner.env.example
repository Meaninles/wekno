# Copy to runner.env. This file is intentionally ignored by Git because model
# and corpus identifiers differ after every independently seeded environment.
WEKNORA_E2E_TENANT_API_KEY=
AGENT_EVAL_SUMMARY_MODEL_ID=
AGENT_EVAL_PROCUREMENT_KNOWLEDGE_ID=
AGENT_EVAL_CORPUS_VERSION=

# Optional external, OpenAI-compatible soft judge. Deterministic hard gates do
# not depend on it.
AGENT_EVAL_JUDGE_BASE_URL=
AGENT_EVAL_JUDGE_API_KEY=
AGENT_EVAL_JUDGE_MODEL=
