# 二开目录结构规范

本文档用于约束 WeKnora 本地二次开发的目录组织。目标是让二开代码和原生代码清晰分离，减少后续升级 WeKnora 时的冲突。

## 总原则

- 二开能力优先放在 `custom/` 或 `internal/custom/`，不要把大段业务逻辑散落到原生目录。
- 能用独立旁路服务解决的，优先放到 `custom/services/<module>/`。
- 必须嵌入 WeKnora 后端时，放到 `internal/custom/modules/<module>/`。
- 必须嵌入 WeKnora 前端时，放到 `frontend/src/custom/modules/<module>/`。
- 原生代码只保留少量稳定注册点，例如路由注册、依赖注册、生命周期启动。
- 每个二开模块独立成目录，模块之间通过清晰接口交互，不互相穿透实现。

## 当前目录结构

```text
C:\weknora
├─ custom/
│  ├─ README.md
│  ├─ document-templates/
│  ├─ services/
│  │  └─ <module>/
│  │     ├─ app/ 或模块实现
│  │     ├─ tests/
│  │     ├─ Dockerfile
│  │     └─ README.md
│  └─ tests/
│     └─ <capability>_e2e/
│
├─ internal/custom/
│  ├─ README.md
│  ├─ bootstrap/
│  │  └─ bootstrap.go
│  └─ modules/
│     └─ <module>/
│        ├─ config.go / types.go
│        ├─ repository.go
│        ├─ service.go
│        ├─ handler.go
│        └─ *_test.go
│
├─ frontend/src/custom/
│  ├─ README.md
│  └─ modules/
│     └─ <module>/
│        ├─ api/
│        ├─ components/
│        ├─ views/
│        └─ tests/
│
├─ migrations/custom/
│  └─ <module>/
│     ├─ 900001_<module>.up.sql
│     └─ 900001_<module>.down.sql
│
├─ deploy/production/
│  ├─ README.md
│  ├─ values-*.example.yaml
│  └─ *migration*.yaml.tmpl
│
└─ docs/custom/
   ├─ 当前实现架构与文档索引.md
   ├─ 当前版本生产更新部署执行手册.md
   └─ <capability>.md
```

模块不要求机械拆成很多子包。以可读性和边界为准：小模块可以在一个 Go package
中使用 `service.go/handler.go/types.go`，复杂模块再拆 repository/scheduler。禁止
为了目录形式把并发状态机分散到多个无清晰所有权的 package。

## 原生代码修改边界

后端尽量只在这些位置增加统一入口：

```text
internal/container/container.go
internal/router/router.go
```

入口形式应类似：

```go
custom.RegisterProviders(container)
custom.RegisterRoutes(v1, guards)
custom.RegisterLifecycle(container)
```

以后新增二开模块时，只改 `internal/custom/bootstrap/` 和对应模块目录，避免继续修改原生路由、服务和业务逻辑。

前端如果需要嵌入菜单或页面，尽量只在这些位置增加统一入口：

```text
frontend/src/router/index.ts
frontend/src/stores/menu.ts
```

新增页面、接口、菜单配置应放在 `frontend/src/custom/modules/<module>/`。

当前已有原生挂接点不只上述两个文件，例如知识列表状态、Wiki 图和移动端入口。
新增改动前先搜索现有 custom import/Hook，优先复用；不要再创建另一套平行注册器。

## 文档与测试要求

- 新模块在 `internal/custom/README.md` 或 `frontend/src/custom/README.md` 登记职责。
- 用户可见能力同步用户指南/API；架构、容量、存储或副本变化同步根 README、
  当前实现架构、Helm 和生产执行手册。
- 文档队列、迁移、删除和多副本功能必须包含并发/race、重试、幂等、租户隔离和
  故障测试，不能只写 happy path。
- Markdown 综合交付文档随主要架构和用户功能同步更新，并执行链接与口径一致性检查。
- 测试 fixture、技能 `SKILL.md` 和模板是运行输入，不在普通文档同步中批量改写。

## 命名规范

- API 前缀：`/api/v1/custom/<module>/*`
- 表名前缀：`custom_<module>_*`
- 环境变量前缀：`CUSTOM_<MODULE>_*`
- Docker 服务名：`weknora-custom-<module>`
- 二开 migration 编号建议从 `900000` 以后开始。
