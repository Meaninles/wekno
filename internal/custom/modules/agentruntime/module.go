// Package agentruntime owns authorized configuration, durable execution
// records, business tools and atomic delivery for the unified AgentScope harness.
// The SDK in custom/services/agent-runtime is the only QA reasoning loop.
package agentruntime
